# FraudML serving configuration. Copy to .env and adjust.
# Either MODEL_NAME (MLflow Model Registry) or MODEL_ARTIFACT_DIR (local)
# must be set. When both are present, the registry path wins.

MODEL_ARTIFACT_DIR=
MODEL_NAME=
MODEL_STAGE=Production
MLFLOW_TRACKING_URI=
FEATURE_STORE_DB=
